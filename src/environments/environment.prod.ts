// Este fichero contiene la configuración del entorno de producción
// La propiedad 'production' se utiliza para habilitar optimizaciones específicas para producción

export const environment = {
  production: true
};
